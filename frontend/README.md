# Frontend Workspace

The canonical project documentation lives in the repository root [README](../README.md).

This frontend workspace contains the React + Vite application for Nexus Archive.

Useful commands:

```bash
npm install
npm run dev
npm run lint
npm run build
```
